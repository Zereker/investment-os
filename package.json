{
  "name": "investment-os-cloudflare-mcp",
  "version": "0.19.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:cloudflare": "wrangler dev",
    "deploy:cloudflare": "wrangler deploy",
    "check:cloudflare": "tsc --noEmit",
    "test:cloudflare": "tsx cloudflare/runtime.test.ts && tsx cloudflare/monthly.test.ts && tsx cloudflare/execution.test.ts",
    "test": "npm run check:cloudflare && npm run test:cloudflare",
    "verify:mcp": "tsx scripts/verify_remote_mcp.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "@types/node": "^26.5.1",
    "tsx": "^4.20.5",
    "typescript": "^6.0.3",
    "wrangler": "^4.115.0"
  }
}
