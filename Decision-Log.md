# Decision Log

本文件记录改变系统方向或产生长期影响的决定。日常定投不逐笔记录。

## 2026-08-04 — 工程清理：检查器 5→3、存证归档惯例、研究工具出插件

### 决定

- 检查器合并：`check_product_contract.py` 与 `check_document_governance.py` 并入 `check_skill_distribution.py`，canonical suite 入口 22 → 20。**断言逐条保真**，合并只去除重复入口样板与一份重复的政策参数拒绝器；退役检查器加入不得复活清单。
- 存证归档惯例：`evals/results/` 只保留**现行**扫描全文；superseded 证据（跨 harness Codex 复核目录、逐场景 samples、历轮叙事）移出工作树、只留 git 历史，README 换为档案表 + 检索指引 + 四条仍然生效的证据处理教训（可观测≠正确、改探针需证据、修复后不改标签、同 harness 会奉承）。历史证据本体不可变，git 历史即归档。
- `fetch_etf_data.py`（238 行）出插件至 `Research/tools/`：季度核查便利工具，政策明文允许全手工回退，与 `drawdown_drill.py`（0.5.1）同类。0.5.1 时「未裁量即保留」，本次所有者裁量迁出。`02-data-contract.md` 采集命令路径同步更新；日期化 lookthrough 记录如实保留当时路径不改。
- `.plugin-version` 0.6.3 → 0.6.4：分发内容变更（插件脚本 8 → 7 个，插件 Python 2,189 → 1,951 行）。

### 原因

所有者 2026-08-04 选定「工程清理 + 研究工具出插件」档：仓库观感精简（工作树约 −2,000 行）而功能零损失；更深的精简（规则真删、渲染层交 LLM、审计外置）均需另行提案或有证据前提，未获裁决不动。

### 规则净增减（复杂度规则要求）

规则零变化。检查器三文件 447 行 → 一文件 424 行（净 −23，入口 −2）；存证工作树 −1,732 行 + README 287 → 135（git 历史全量保留）；插件迁出 238 行至源仓库。

### 非交易声明

本决定只合并检查器入口、归档历史证据与迁移一个研究便利工具，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — 所有者裁决：LLM 负责投资判断为预期设计，套件对齐收编

### 决定

- 认可近期合入的方向为产品预期设计：**代码验证事实并保护执行，LLM 负责投资判断**。北极星由「相同输入得到相同结论」改为「结论可解释、证据可追溯、执行可验证」。投资判断长在 Skill 与规则里，不为每个分析细节新增代码、字段、状态机或 Skill。
- 收编而非回卷：canonical suite 对齐新契约——router 点名 `validating-drawdown-state` 与 `financial-agent-discipline`；`test_skill_system` 的强制子技能图断言改为「router 必须能点名其路由或加载的全部 Skill」；注册五个新行为场景（daily-review-analysis-with-incomplete-data、daily-review-hold-is-success、daily-review-news-does-not-drive-output、daily-short-command、investment-agent-personality）；`check_product_contract` 改钉新契约的承重句（LLM 判断边界、非机械北极星、IC 结论非 Broker 授权、默认分支 HEAD 规范来源、不可变分发快照、`F` 不得默认为零）。被改写句子的职能已有新家者不复原：旧制例外与 `02-*`>`03-*` 顺序钉在宪法，来源义务及其后果钉在 `financial-agent-discipline`。
- 操作手册指向产品契约第 10 节的「九项条件」改为不带计数的「授权条件」（该节现为七项，计数指针已陈旧）。
- 证伪回路：clean-session 行为 eval 是该边界的探测器。若真实 Harness 扫描显示 LLM 判断不可解释、不可追溯或绕过执行门，本裁决回 Research 重审。Real Harness behavior 仍为 NOT YET VERIFIED。

### 规则净增减（复杂度规则要求）

投资参数、阈值、公式、授权语义零变更。router 增两句点名散文；测试与检查器为钉子替换（净约 ±0）；行为场景注册 +5。

### 非交易声明

本决定只把测试、检查器与文档对齐到已裁决的产品边界，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — 三档真删规则裁决：周度压缩批准，Benchmark 简化与季度并问未采纳

### 决定

- **批准**周度复盘压缩为「周度行为检查点」：删除与每日复盘/月度流程重复的 A–D 节（账户、配置、数据检查），保留行为与执行回顾（含 Research 边界一条）与四类工作项输出。本账户已被证明的最薄弱环节是行为（Journal 2026-07-31），行为回看是唯一保留的周度职能。提案与证伪回路见 `Research/2026-08-04-weekly-review-compression.md`。
- **未采纳** Benchmark 现金计息简化（`Research/2026-08-04-benchmark-cash-flat-rate.md`）：现行门槛 + NAV 缩放精确模型保留。
- **未采纳**季度六问并三问（`Research/2026-08-04-quarterly-questionnaire-slim.md`）：六问保留，问卷仪式性确认价值由所有者裁量高于维护成本。

### 规则净增减（复杂度规则要求）

操作手册周度部分 71 → 约 30 行（净 −41）；其余规则不变。

### 非交易声明

本决定只压缩一段流程清单并存档两份未采纳提案，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — 运行时代码瘦身：删除死代码，研究工具出插件

### 决定

- 删除 `behavior_packet.py`（71 行）：产品从不产出该 schema，全仓唯一使用方是一个测试；对应测试收缩为契约/语料/回放校验。
- `drawdown_drill.py`（269 行）迁出插件至 `Research/tools/`：机制验证使命已完成——十年回放证据在 Research 存档、七项不变量已验证、档位常量由 `mirror_tests()` 逐元组比对；改档位需提案时仍可运行。四份研究文档的复现命令路径同步修复。
- `check_release_governance.py` 并入 `check_skill_distribution.py`，canonical suite 少一个入口；`test_skill_system` 删除与产品契约检查重复的三条断言。
- `fetch_etf_data.py` 保留：季度核查便利工具，政策允许全手工回退，去留属所有者裁量，未裁量即保留。
- `.plugin-version` 0.5.1：分发内容变更（插件脚本 10 → 8 个，插件运行时 2,502 → 2,162 行）。

### 原因

代码的三类正当职能是镜像（同输入同答案）、哨兵（守不可逆动作）、探测器（量遵循率）；死代码与使命已完成的一次性验证工具不属于任何一类，插件分发只应携带运行时真正需要的部分。

### 非交易声明

本决定只删除与迁移代码，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — 内容级精简（一档：论证外迁；二档：语义去重）

### 决定

- 一档（零删除）：宪法中与 Research/ 提案重复的论证散文——梯度选型对比、终点 25% 与归零依据、定额释放论证、再平衡分工实测——替换为一句归纳加 research 链接；规则本体、分档表与「2008 型深跌无弹药」等已知风险披露原文保留。
- 二档（语义不变）：日报契约并入每日复盘为单一部分（取两者并集，PR #62 的全部新增保留）；回撤分档表单源化到宪法（部署框架与字段字典改为指针）；回补/提高倾斜约束在宪法内单源化；产品契约的日/月清单改为指向操作手册；执行边界九项条件单源化到产品契约。
- 「不丢失」约束经所有者批准由「原文保留」放宽为「语义保留」；外迁论证的原文全部存在于 Research/ 对应提案。

### 规则净增减（复杂度规则要求）

- 宪法 517→480，操作手册 964→811，产品契约 235→213；规则与契约合计 2,304→2,092 行（约 −9%）。
- 剩余体量为互不重复的规范性内容；进一步精简属于真删规则（周度复盘仪式、Benchmark 现金模型精度、季度问卷长度等），须按治理逐项提案与所有者批准。

### 非交易声明

本决定只压缩表述与去除重复，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-03 — 规则优先重组：29 份政策文件合并为 4 份规则 + 契约，检查器回归可执行断言

### 决定

- 编号政策参考由 29 份合并为 4 份规则文件加契约与适配层：`00-constitution.md`（IPS + 投资宇宙 + 目标配置 + 转型计划 + 倾斜框架 + 登记表）、`01-operating-manual.md`（日/周/月/季/年流程 + 日报契约 + 部署框架 + 状态重建 + IC 清单）、`02-data-contract.md`（数据操作 + 注册表 + 质量闸门 + 字段字典 + 穿透核查程序）、`03-journal.md`（日志 + 经验）。`project-contract.md` 与 `production-contract.md` 去重合并为 `product-contract.md`。日期化核查记录移至 `references/records/lookthrough-YYYY-MM-DD.md`。
- **合并为逐条保真的串联加去重**：每一条规范性规则原文保留，只更新交叉引用、去除跨文件完全重复的陈述；不改变任何投资参数、阈值、公式或授权语义。
- 权威顺序更新为 `00-constitution.md` → `01-operating-manual.md` → `03-journal.md`；`02-data-contract.md` 约束数据可用性，不创造投资参数。为保持冲突解决语义不变，显式保留一条旧制例外：宪法的转型计划部分与操作手册冲突时以操作手册为准（原 `02-*` 优先于 `03-*`）。
- 新增纯规则 Skill `financial-agent-discipline`：把行为契约七原则写成可观测、带后果的行为规范，不含任何投资参数或流程；`behavior-contract.yaml` 声明为其机器镜像。所有任务在领域工作前先适用该 Skill。
- 检查器回归可执行断言：`check_policy_consistency.py` 删除约百条散文 require/forbid 钉子，保留政策数学性质测试、隐私门、冻结状态正则、退役文件检查与一个小型已知陈旧词汇禁令；新增 `mirror_tests()` 用 importlib 加载实际运行时模块并逐元组比对档位与常量（取代只看首尾两行的字符串钉）。`check_document_governance.py` 收缩为结构检查（规则文件存在、标题无版本号、`NOT YET VERIFIED` 声明存续）。
- `.plugin-version` 自 `0.4.0` 升至 `0.5.0`：分发结构发生实质变更（文件合并、新增 Skill、入口更新）。

### 原因

- 系统初心是「只定义规则」。29 份互相引用的政策文件需要 629 行检查器看守一致性，且仍出现了同一文件内自相矛盾的段落（v4.4 残留文案，2026-08-03 修复）——需要巡逻队的宪法本身就说明它太大。
- 散文钉子断言「某句话还在」，不断言行为正确，并在无害改写时误报；参数与实现的真实联结应当是模块常量比对，而不是字符串匹配。
- 行为契约七原则对任何碰真实资金的 Agent 都成立，与具体组合参数无关，应作为可独立分发的纯规则资产存在。

### 规则净增减（复杂度规则要求）

- 规则文件数：29 → 9（4 份规则 + product/agent-execution 两份契约 + 1 份 Claude 入口 + 2 份 Harness 映射），另 records/ 存放日期化核查记录。
- references 行数基本持平（串联保真，净 −16 行）；治理检查器净 −201 行；新增纯规则 Skill 约 +150 行。

### 非交易声明

本决定只重组文件结构、检查器实现与分发版本，不改变投资宇宙、配置、资金公式、回撤规则、护栏或任何交易权限，不授权任何订单。

### 复核条件

- 下一次政策修订时：确认合并文件的部分内定位（宪法六部分、手册九部分）是否仍可维护；若单文件内部再次出现自相矛盾段落，重议是否需要按变更节奏拆分（宪法 vs 登记表）。
- 零丢失审计记录见对应 PR；若后续发现任何一条规则在合并中丢失，按缺陷处理并立即恢复原文。

## 2026-08-02 — 分发出去的 Skill 以「加载即执行」为权威，不在运行时取最新

### 决定

- Skill 层不再要求 Agent 解析仓库 HEAD 或获取最新政策。**加载进会话的那份内容与版本，就是本次要执行的内容**；版本身份取自 `.plugin-version`。
- `using-investment-os` 的 Core rule 与 Mandatory start、两份 Harness 映射表、以及各 domain skill 中「从 repository HEAD 读取」的表述，统一改为「读取随本 Skill 分发的政策文件」。
- 会话不得声称、也不得暗示自己这份分发是最新的。分发是否落后属于**发版问题**，不属于会话内可自证的问题。
- `AGENTS.md` 面向**在仓库上作业的 Agent**（有 git、开分支、提 PR），其 HEAD 协议保留不变；`PROJECT.md` / `PRODUCTION.md` / `README.md` 中「现行政策以默认分支 HEAD 为准」的规范来源陈述保留不变。
- `check_product_contract.py` 增加断言：分发 Skill 中不得出现 `repository HEAD`（`auditing-investment-os` 例外，它审计仓库本身）。
- 行为场景 `manual-figures-are-not-authority` 的必需行为相应改为「说明所依据的政策来源，且不得声称取过更新的版本」。

### 原因

- 通过 Harness 市场安装的插件**根本没有 git 仓库**，原措辞会让每一个正经安装的会话停止工作。
- 逻辑上循环：用一份可能已过期的 Skill，去指示 Agent 校验这份 Skill 是否过期。过期的分发携带的也是过期的校验指令。
- 实测证据：要求输出 HEAD SHA 后，Agent 两次运行报出不同 revision，其一是特性分支而非默认分支，且 `git rev-parse` 只反映上次 fetch，无法证明「最新」。结果是把原本含糊但诚实的来源引用，换成了精确却未经验证的 commit id——制造虚假权威。

### 三层语义（独立审阅意见后补充）

初版只改了 Skill 层，导致同一个安装包出现两套 Mandatory Start：`CLAUDE.md` 等文件仍要求「每次从默认分支 HEAD 读取」，而 Skill 已改为「分发包内容即权威」。现统一为三层，互不混淆：

| 层 | 含义 | 由谁确立 |
|---|---|---|
| 规范权威 | 默认分支 HEAD 标识现行投资政策，政策变更在此发生 | 仓库 |
| 会话实际输入 | 本次安装所分发的政策文件；会话只读它，不在运行时取更新 | 安装 |
| 来源证明 | 每个已发布 `.plugin-version` 对应一个 Git tag，该 tag 记录这份分发切自哪个 commit | 发版时 |

会话报告自己运行的分发版本，不报告自行解析的 commit；审计者按 版本 → tag → commit 离线还原。分发落后于默认分支属于发版问题，会话无法自证、也不得声称已确认。

同步更新：`CLAUDE.md`、`PROJECT.md`、`PRODUCTION.md`、`README.md`、`docs/SKILL-DISTRIBUTION.md` 与两份 plugin manifest 描述；`check_skill_distribution.py` 断言三层标题存在。

`.plugin-version` 自 `0.1.0` 升至 `0.2.0`：Skill 契约的权威来源语义发生实质变更。（`0.1.0` 实际从未打 tag 发布。）

### 非交易声明

本决定只修正 Skill 层的权威来源与版本语义，不改变投资宇宙、配置、资金公式、回撤规则或任何交易权限，不授权任何订单。


## 2026-08-02 — Skill 分发版本与政策版本解耦

### 决定

- 删除 `07-Releases/`。长期有效的系统决定统一进入 `Decision-Log.md`，缺陷与防复发措施进入 `BUGLOG.md`，具体实现历史由 commit、PR、Git tag 与 GitHub Release 留痕。
- 当前投资政策只由默认分支 HEAD 与精确 commit SHA 标识，不再用仓库内的 `vX.Y` 发布文件表示政策版本。
- Skill / Plugin 分发保留独立 SemVer，由 `.plugin-version` 作为唯一来源并同步到 Claude Code 与 Codex manifest；首个独立分发版本为 `0.1.0`。
- 行为场景定义与行为执行分开：CI 只验证 YAML 结构、Skill 引用、覆盖和隐私。**真实 Agent 行为 Eval 尚未验证**；只有 clean-session actor 与独立 verifier 实际运行通过后，才能宣称对应 Harness 的行为覆盖。

### 原因

- `07-Releases/` 与现行政策及 `Decision-Log.md` 重复，可能成为脱离 CI 的第二份参数来源。
- 插件 SemVer 服务安装、升级、兼容和回滚，不等于投资政策版本。
- 场景文件合法只证明测试定义存在，不能证明 Agent 在压力下真的遵守 Skill。

### 非交易声明

本决定只修正治理、分发版本和测试陈述，不改变投资宇宙、配置、资金公式、回撤规则或任何交易权限，不授权任何订单。


## 2026-07-31 — 运行时多源数据与决策留证

### 决定

- 删除仓库中的中央issuer/GICS全量表，不再把项目作为外部金融数据库维护。
- 日常巡检运行时分别读取IBKR、基金管理人、身份源和权威分类源；不同插件无需由单一供应商承载。
- 普通价格、成分和分类变化不修改仓库。
- 只有形成真实投资决策时，才在快照中保存当次来源、`as_of`与近似处理说明。
- 历史快照继续只增不改，以保证既有决策可复算；数据源缺失或冲突时失败关闭。

### 非交易声明

本决定只调整数据边界与审计实现，不改变任何风险阈值，不授权任何订单。

## 2026-07-31 — v4.0 证据驱动的结构修正与简化

### 决定

- SOXX重述为有硬上限的半导体板块倾斜（行业beta，不是alpha）；永久硬上限6%，10%/12.5%/15%历史阶段作废。
- 穿透护栏语义修正：约束自主倾斜新增，不阻断SPYM/QQQM例行路径；合并暴露每季度手工核查披露。
- 新增回撤部署条款：SPYM相对历史最高收盘回撤≥15%/25%/35%时，现金下限分档降至`10/8/6%+U`并部署至Core正缺口；不依赖估值数据，每档每周期一次。
- 15%结构性现金的预期成本（约0.45–0.75pp/年）写入IPS作为显式决策。
- 估值框架降级：等级只影响`T`与`VERY EXPENSIVE`下的`B`；`D/B`不再被估值数据缺失阻塞。
- 删除Look-through Evidence Bundle验证器/解析器/对抗测试/JSON契约（约3,300行）与含真实账户数据的历史Bundle快照；改为`08-Data/LOOKTHROUGH_CHECK.md`季度手工核查。
- 新增影子基准SB-1（满仓政策组合67/33）与SB-2（100% SPYM），仅报告。
- 生命周期简化为`Hold / Frozen / Exit Review`；Add Candidate Packet机制退役。

### 原因

- 实测合并穿透（2026-07官方持仓）：目标Core自身半导体约18%（已越15%线），SOXX=15%端点半导体约31.7%、IT约47.4%，在自身护栏下不可达；15%目标只产生治理开销。
- 估值关键字段长期Red使v3.5实际恒为N/A，战略迁移被无限期挂起（BUG-007复发形态）；且现金下限12%使「危机弹药」叙事名不副实。
- 约3,700行验证器保护的决策每季度只需15分钟手工核查即可覆盖；复杂度本身已成为最大操作风险。

### 非交易声明

本决定只更新治理、计算与流程规则，不授权任何订单。发布时`A_actual`高于6%上限的漂移状态按规则冻结新增、不自动卖出。

### 复核条件

- 每季度：手工穿透核查、回撤部署周期状态、SOXX相对等额SPYM表现。
- 每年：6%上限、15%现金、估值框架职责与影子基准累计对比。

## 2026-08-01 — v4.1 Policy Benchmark 现金模型由日频改为月频

### 决定

- 基准现金袖套收益改为以月初袖套值为本金的单一月度公式；退役已入账/未入账拆分、日频递推与入账日科目转换。
- 保留全部不变量：按经纪商官方规则重算假设袖套、只在月初重置权重、利息不在月内复利、输入缺失记 `N/A` 不得用 0% 或上月值替代、外部现金流不进入收益分子。
- 不改变目标配置、阈值、护栏、生命周期、资金通道或估值职责。

### 原因

原规则要求逐日利率序列，缺一日则整月作废，而仓库不存储运行时数据、agent 每会话冷启动，实际无机制生产该序列——该字段因此结构性恒为 `N/A`（BUG-007 的同一形态：闸门严到永不开启）。日频与月频的数值差异约为组合净值的 1e-6 量级，对该基准在年度审核中的用途无决策影响；影响数值的免息门槛与 NAV 比例缩放两项完整保留。

### 非交易声明

本决定只更新一项只报告用的计算方法，不授权任何订单。

### 复核条件

年度审核：确认该字段是否已稳定产出真实数值；若仍长期 `N/A`，需重新审议基准现金收益是否值得建模。

## 2026-08-01 — v4.2 估值子系统整体退役

### 决定

- 删除 `02-Operating-System/ETF-Valuation-Framework.md` 整篇；系统不再持有任何估值判断。
- 退役 `T` 战术加速通道；资金通道由四条收为三条（`D` / `B` / 回撤部署）。
- 取消 `VERY EXPENSIVE` 对 `B` 的暂停；`B=min(S/R,G)` 完全决定基线，没有任何判断性闸门。
- 价格只用于计量、执行与回撤定档，不定义贵便宜，不生成新增资格。
- 不改变目标配置、护栏阈值、SOXX 治理或回撤档位。

### 原因

四个输入字段全 Red 且无法转 Green（2026-08-01 实测：QQQM 官方页是纯 SPA，整页无 P/E 字样；SOXX 官方页只有 Trailing P/E，规则禁止用它判便宜）。更根本的是历史百分位死结：基础等级依赖同口径 5–10 年月末序列，无源可得，即使今天开始抬录也要到 2031 年才生效。框架输出因此恒为 `N/A`——与 BUG-007、与 v4.1 修掉的 benchmark 日频模型是同一形态。v4.0 证据文档已判定 Forward P/E 百分位是弱择时工具；回撤部署条款以零数据依赖承担了同一职能，且已用十年真实数据验证。

### 取舍

失去「高位刹车」，但该刹车从未生效过——不损失实际职能，损失的是名义选项。超出 `B` 的加速只剩回撤档位一条机械路径；主动加速须走规则例外的完整 IC。

### 非交易声明

本决定只删除规则与计算路径，不授权任何订单。

### 复核条件

年度审核：用 SB-1 / SB-2 影子基准审判「没有高位刹车」是否付出了代价。若确有代价且届时存在合格数据源，可走正式提案重新引入。

## 2026-08-01 — v4.3 回撤部署 T1 触发线由 15% 下调至 10%

### 决定

- T1 触发线 `DD ≥ 15%` → `DD ≥ 10%`。T1 的临时现金下限（`10%+U`）、T2、T3、每档每周期一次、新高重置、SPYM 触发口径全部不变。

### 原因

十年 13 次回撤中 [10%,15%) 区间为空，10% 触发在历史样本上零误触发；而现行规则下一次触底 12% 的回撤完全不动用弹药。所有者判断空带是小样本假象，宁可买贵也不愿在中度回撤中完全缺席。

### 反对证据（已提交，未被采纳）

四次深度回撤中等到 15% 严格更优——跌破 10% 后全部继续跌至 19%–34%，提前开火每次贵 7–9%（2018-09 +6.8%、2020-02 +8.4%、2022-01 +7.8%、2025-02 +9.1%）；且 9.96%/9.78%/9.13% 三次近失擦线而过，10% 线正压在该簇上。

### 已知机械后果

同一周期内 T1 在 10% 消耗后，若随后深跌至 20%，在 10%–25% 区间不再有额外解锁；旧的 15% 线恰好覆盖该段。

### 非交易声明

本决定只更新触发阈值，不改变目标配置、现金下限、护栏或任何公式，不授权任何订单。

### 复核条件

每次 T1 触发须在 Journal 记录触发日 `DD`/收盘、本周期谷底与价差。累计三次后年度审核计算「10% 开火」相对「等到 15%」的平均劣势：稳定为负则必须重议；若出现一次触底于 10%–15% 后恢复的周期，则保险价值兑现、确认保留。

## 2026-08-01 — v4.4 回撤部署改为六档等额分批

### 决定

- 档位由三档不均匀（10/25/35 → 下限 10/8/6）改为六档等额（10/15/20/25/30/35 → 下限 13.5/12/10.5/9/7.5/6），每档释放 1.5pp of NAV。
- 总弹药 9pp 与终点 `6%+U` 不变；触发口径、每档每周期一次、新高重置、部署额公式形状、部署去向全部不变。

### 原因

原有的「分批限价执行」只是订单执行层面，部署金额仍是一次算出；而执行约束明写「不一次性满仓」，与 T1 公式直接矛盾。实测一次 T1 事件部署 4pp of NAV 落在单一价位，且 v4.3 刚把 T1 下调至 10%，使这 4pp 落在最差入场点。分批把择时问题转成机械规则——跌得越深买得越多，不预测底部。不引入任何「谷底指标」：v4.2 退役估值子系统的理由同样适用。

### 意外收益

等额步进使现金水位几乎单射地对应已执行档数，显著强化了 State-Reconstruction 中唯一未验证的「现金水位自证」信号。

### 已知代价

已执行状态从 3 档变 6 档。缓解见发布说明与 State-Reconstruction 第 4 步。

### 非交易声明

本决定只更新档位结构与各档临时现金下限，不改变目标配置、总弹药、终点或触发口径，不授权任何订单。

### 复核条件

首个完整回撤周期结束后，Journal 须能重建每档触发日与部署额。若水位自证未能唯一确定已执行档数，或六档跟踪造成遗漏/重复，年度审核必须重议档位结构。

## 2026-08-01 — v4.5 「回补至目标」与「提高倾斜」拆分

### 决定

- 「追加」拆为两条路径，判别标准只看 `A_execution_cap` 动没动：**提高倾斜**（推进执行档，风险预算扩大）仍走完整 IC，闸门一字不改；**回补至目标**（执行档不动，买回被市场打下去的权重，风险预算不变）走月度例行路径。
- 回补上限 `min(U×V, (min(A_execution_cap, A_stage) − A_actual)×V)`，须同时满足五项约束：不超上限、当季穿透核查有效、资金只来自 `U`、科技/发行人护栏不失守、实时账户读取成功。任一不满足即输出 `0`，不得部分执行。
- 目标配置、`A_stage`、`A_execution_cap`、回撤分档与所有资金公式均不变；不新增资金、不放宽上限。
- 月度流程由七步改为八步（新增第 4 步回补判定）；Position-Registry「追加闸门」更名「提高倾斜闸门」，条款内容未改。

### 原因

SOXX 跌破 6% 时缺口进入 `U`，现金目标升至 `15%+U`、回撤绝对下限升至 `6%+U`，那笔钱被正确隔离；但它的**出口**此前要求完整 IC，而 IC 需要人在崩盘中主动发起。结果是不对称：SPYM/QQQM 跌了由 `D`/`B` 机械买回，SOXX 跌了要等人。回补与「QQQM 跌了买 QQQM」概念上同类——目标没动，只是市场把权重打下去了，却被当成扩张风险预算来把关。这是分类错误。

### 已记录的反方

SOXX 是集中度载体（6% 时合并半导体约 24%）；崩盘时当季核查很可能过期；半导体高 beta，机械回补等于在相关性飙升时加最波动的袖套。前两点由约束 4 与约束 2（fail-closed）处理，第三点是自觉承担的取舍——「跌了就不补」等于让市场单向决定倾斜大小，那是隐性择时。

### 当前无实际影响

`A_actual` 高于 6% 硬上限，`U=0`，回补额恒为 0。这是前瞻性规则。

### 非交易声明

本决定只拆分闸门归属并新增回补的例行路径，不改变任何目标、上限或公式，不授权任何订单。

### 复核条件

首次真实回补后，Journal 须记录回补前后 `A_actual`/`U`、当季核查日期与结论、交易后合并半导体暴露。若机械回补把合并半导体推过护栏而无人察觉，或「当季核查有效」在真实危机中长期为假致通道从未打开，年度审核必须重议。

## 2026-08-01 — v4.6 回撤阶梯改为四档梯度，25% 处把现金全部投出

### 决定

- 档位由六档（10/15/20/25/30/35，每档 1.5pp）改为**四档 10/15/20/25，梯度释放 1.5 / 3 / 4.5 / 6pp**。
- **绝对下限由 `6%+U` 改为 `0+U`**：现金在 T4 处全部投出。`U` 仍单独排除在外。
- `DD` 超过 25% 后不再解锁任何档位，继续下跌不产生新的部署授权，**也不借款**。
- 新增执行约束：下单量为整股且含佣金后现金不得为负；归零后的后续费用由外部入金补足，不得转为融资借款。
- 触发口径（SPYM）、常态下限 `12%+U`、每档每周期一次、新高重置、部署去向、**IPS 无杠杆原则**全部不变。

### 原因

三条查证结果：

1. **最深两档几乎从不触发**：SPYM 十年回放的 4 个触发周期，六档平均弹药利用率仅 58%，四档 81%。
2. **6% 从未被论证过**：它是 v4.0 那条 `10% / 8% / 6%` 序列的末端，仓库中没有任何文件为这个数单独给过理由。而 Constitution 明写 15% 现金每年成本 0.45–0.75pp，条件是现金真正承担弹药职能。
3. **等额把最大的一笔放在最差入场点**：同为 15pp 弹药，等额首笔 3.75pp 落在 `DD` 10%；梯度 1:2:3:4 把首笔压回 1.5pp，同时把 60% 弹药放在 T3/T4，加权入场回报由 22.5% 升至 26.7%。

十年收益贡献：六档 @6% 下限 6.58pp → 四档梯度 @0 下限 **11.60pp**。

### 关于杠杆

本轮曾讨论「跌 30% 贷款也要加仓」，所有者最终选择**不做**，IPS 无杠杆原则保留。分析记入提案第 4 节，含一处自我纠正：按 Reg-T 25% 维持保证金，借入 30%×NAV 需再跌约 69% 才强平，**强平在该量级上不是现实风险**；真正代价是利息不对称、还款与现金重建争夺同一笔入金、以及 IPS 冲突。未来重议须走独立提案。

### 已知代价

2008 型深跌（标普约 −56%）在 25% 后无任何弹药；IPS 现金职能第②条「行为缓冲」在最深档消失（不修改 IPS 原文，让渡的是缓冲垫不是原则）；梯度在浅回撤中投出少于等额（十年 43.5pp vs 48.75pp），以准度换总量，净账仍胜。

### 非交易声明

本决定只更新档位数量、每档定额、阶梯终点与绝对下限，不改变目标配置、触发口径、无杠杆原则或任何其他公式，不授权任何订单。

### 复核条件

首个完整回撤周期结束后：若 `DD` 超过 25% 且事后判断「当时有弹药会更好」，或现金归零后发生本可用小额现金避免的被迫操作，或连续两个周期利用率仍低于 60%，或现金水位未能唯一确定已执行档数——任一成立，年度审核必须重议。

## v3.x 决策存档（已由 v4.0 取代）

以下条目全部不具有现行效力，仅供审计追溯。v1.x–v3.5 的完整正文随仓库历史重建已不可恢复，本节是唯一存档；现行规则一律以工作树文件为准。

| 日期 | 决定（一句话） | 现状 |
|---|---|---|
| 2026-07 | 从选股系统升级为财富管理系统：Cash 15% + SPY 42% + QQQ 28% + Alpha 15%，以新增资金渐进迁移 | 载体改为 SPYM / QQQM；Alpha 端点由 v4.0 作废 |
| 2026-07-30 | v3.3 规则闭环与 SOXX 分类：Alpha 恢复为 0%–15% 机会预算，`SPYM = 57%−A`；SOXX 归入 `Alpha / Observation`；启用穿透集中度软护栏 | Alpha 命名与 15% 预算由 v4.0 作废；护栏语义由 v4.0 修正 |
| 2026-07-30 | v3.4 SOXX Strategic Alpha：SOXX 成为唯一 Alpha 载体，长期硬上限与最终治理阶段定为 15%，当前阶段 6%；Policy Benchmark 现金收益改用假设 15% 现金的 IBKR 规则模型 | 15% 长期上限由 v4.0 作废（6% 成为永久硬上限）；Benchmark 现金模型仍有效 |
| 2026-07-30 | v3.4.1 Reliability Patch：修复 `A_basis`/`U` 执行公式；Benchmark 改月初重置、月内递推计息；生命周期统一为 `Frozen — DATA GATE`；执行档逐档推进 | 公式与 Benchmark 修复仍有效；`Frozen — DATA GATE` 生命周期与 10%–15% 档位由 v4.0 退役 |
| 2026-07-30 | v3.4.2 Data Gate Operationalization：以 Look-through Evidence Bundle v1.4 保存三只 ETF 官方全量持仓，由验证器从底层重算覆盖率与护栏上下界 | Bundle 验证器整体由 v4.0 退役，改为季度手工核查 |
| 2026-08-01 | v3.5 三只 ETF 估值与新增资金节奏：估值范围固定为 SPYM/QQQM/SOXX；Forward P/E 只与自身历史比较，等级边界 `<20 / 20–70 / 70–90 / ≥90`；回撤不再与 P/E 相加成 Opportunity Score | 整个估值框架已由 v4.2 退役 |
| 2026-08-01 | v3.5.1 估值数据质量与资金权限修正：低质量代理只标 `PROXY CAUTION`，不得改变资金安排；`D` 不因估值关闭 | 已被 v4.0 的估值降级吸收，并随 v4.2 退役一并失效 |
| 2026-08-01 | 固定 SPYM 42% / SOXX 15% 目标仓位，废止 `57%−A_basis` 动态公式（并行会话产生） | 与 v4.0 实测证据直接冲突；2026-08-01 合并时按所有者批准的 v4.0 裁定作废。若要恢复，须先推翻 v4.0 的穿透证据（合并半导体约 32%、IT 约 47%）并走正式版本流程 |

## 2026-08-01 — 一次性券商警报修复授权

- 账户所有者在当前会话明确授权 Agent 修复券商中的回撤警报，使其重新符合“恰好一个警报并指向下一可用档”的状态不变量。
- 操作范围仅限修复该警报；未授权下单、修改持仓或执行其他券商写操作。
- 操作后通过权威 read-back 验证警报唯一、启用且与当期下一可用档一致。
- 本次授权不跨操作、不跨会话继承，也不构成未来的常设权限。后续券商写操作适用 Execution Runtime 的单次操作授权与 read-back 契约。
- 本记录只保存治理理由，不保存账户标识、警报 ID 或其他私人运行时状态。
