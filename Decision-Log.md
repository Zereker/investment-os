# Decision Log

只记录会长期改变产品方向、政策权威或运行边界的决定。实现过程、文件迁移、检查器合并、行数统计和已关闭缺陷留在 Git 历史与 PR 中。

## 2026-08-04 — 行为规则 0.9.1–0.9.7 变更汇总，并记入两笔形态欠债

本条补记宪法复杂度规则要求的净增减报告：0.9.1 至 0.9.7 共七个版本改动了 canonical `SKILL.md`，此前只在 PR 描述中说明，未落到本文件。

- 决定：追认以下七版行为规则变更，并把其中两处形态问题记为已知欠债，暂不改动。
  1. **0.9.1 Rule 5 去掉不可满足谓词** —— 原文要求授权来自「已验证的账户所有者」，而系统无任何身份验证机制。五个样本、三种写法均失败；拿掉该谓词后三样本通过。
  2. **0.9.2 取舍阀门与两条自检句** —— 新增作用域限定的 tradeoff 声明（规则治理真实账户状态与不可逆动作；规定前提的示例与假设正常作答），Rule 1 与 Rule 7 各加一句自检问句，description 写入要防的失败形态。
  3. **0.9.5 Rule 5 补回权限归属** —— 0.9.1 的纯禁止写法把「最终权限属于账户所有者」挤掉了，由 Codex 跨 harness 复核发现。改为两半都必须明说。
  4. **0.9.6 Completion 恢复来源与状态两项** —— Delete Sprint 压缩 Completion 时同时删掉「点名政策来源」与「说明决策状态」；Codex actor 因此不做这两件事，Claude actor 靠训练习惯掩盖了缺口。同版本把一条评分误报（把拒绝式的零数量判为可执行数量）改为只针对可执行指令。
  5. **0.9.7 收尾义务限定作用域并要求枚举双向路径** —— 0.9.6 的全局收尾义务与简洁约束对撞，且两个模型都只报被问的那条路径。改为只在阻断路径或涉执行授权时出现，并明确买入与卖出是两条路径。
- 原因：全部七版均由真实行为证据驱动，无一处出自设想。其中三处缺陷由跨模型证据确认——同一失败在 Claude actor 与 Codex actor 上同时复现，才判定为规则缺陷；只在单边出现的两处（`no-inherited-agent-approval` 权限归属、`daily-short-command` 能力解析）按此判据留档不动，等下一轮双边数据。
- 对现行政策或产品边界的影响：投资参数、阈值、公式、投资宇宙与授权语义零变化。改变的只是 Agent 行为规范的措辞与作用域。
- 规则净增减（宪法复杂度规则要求）：`SKILL.md` 119 → 129 行（净 +10），期间修复八处经证据确认的行为缺陷，平均每处 1.25 行。七条规则字数 20–93 词，中位 45；结构未新增层级、分类或文件。
- **已知形态欠债（本条明确记账，暂不动手）**：
  - Rule 5 现为 93 词，是规则中位数的两倍，且需要一句元说明（「只说后半句会丢掉前半句承载的边界」）才能被正确执行。一条规则要靠元说明才成立，通常意味着它要求了两件张力相反的事。
  - Completion 现为 141 词，并需要专门一段说明它在常规复盘中不适用、以及它与简洁约束的冲突关系。规则需要另一段划定不适用范围，是复杂度的典型形态。
  - 不现在动手的理由：两处刚被跨模型证据验证有效（0.9.7 六场景全过，双边复现的缺陷已消除）。本会话已三次证明，在没有新证据时改动已验证的规则，只会用一处失败换来另一处失败。
- 证伪或复核条件：下一轮双边扫描若在 Rule 5 或 Completion 相关场景出现新失败，即为动手重构这两处的证据；若连续两轮双边扫描均无相关失败，则该欠债降级为纯风格问题，可在年度审核处理。若本条追认的任一变更被证明削弱了控制而非加强，回退该版并记录。
- 非交易声明：本条只补记已发布的行为规则变更与已知欠债，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — 回撤档位判定改以成交记录为主信号；回补至目标免 IC 但每次披露暴露

- 决定：
  1. 档位已执行判定改以**周期内 IBKR 成交记录**为主信号（按金额对应梯度定额、日期在达档之后归属）；现金水位降为辅助校验且不得单独判定；警报指针仍只作 actual pointer 被审计。
  2. 半导体 15% 护栏线只 gate **提高倾斜**；**回补至目标**走例行路径免 IC（不扩大风险预算，上限仍是既有执行档），但每次回补必须报告交易后合并半导体暴露（原「首次」改为「每次」）。
- 原因：
  1. 现金权重分母是实时 NAV，回撤中 NAV 下跌使现金权重机械上升，部署后读数系统性偏高。按本梯度测算，`DD=10%` 执行 T1 后现金约 14.9%（表值 13.5%）、`DD=25%` 打光 T4 后约 13.1%（表值 0%），四档全部偏向「更少档已执行」，会反复重触发浅档，把弹药消耗在浅回撤——正是 1:2:3:4 梯度要防止的失败。成交记录是唯一既独立于 NAV 漂移、又不让被审计对象自证的证据。
  2. Core 自身半导体暴露即约 18%，该线常态触发；而回补按定义无法把 `A_actual` 推过既有执行档，本线要防的集中度爬升已由执行档锁死。原文「任何 SOXX 倾斜的新增」与回补五项约束（只列 IT 50%、发行人 10% 两条冻结线）在同一部分内可作两种读法，压力时刻会出现规则不确定。
- 对现行政策或产品边界的影响：投资参数、阈值、公式、投资宇宙与授权语义零变化。改变的是档位状态如何被重建，以及回补的治理路径由「文本歧义」变为「明确的例行路径 + 每次披露」。
- 规则净增减（宪法复杂度规则要求）：手册状态重建部分净 +1 行（删去现金水位映射表 5 行，新增偏差说明与归属规则）；宪法两处为同行改写，无净增。
- 证伪或复核条件：首次真实回撤触发时，必须在当前私有会话核验成交记录归属是否给出了正确档位；若成交归属无法与档位对应（例如同日既有月度 DCA 又有回撤部署且金额接近），该判定路径回 Research 重审。若回补被发现在执行档不变的前提下仍推高了合并半导体暴露，恢复本线对回补的 IC 要求。
- 非交易声明：本决定只改变状态重建的证据优先级与回补的治理路径表述，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — Investment OS 收缩为一个规则 Skill

- 安装产品只公开 `skills/investment-os/SKILL.md` 一个 Agent 入口。
- 编号 references 保存投资政策；其他 `skills/*/` 目录只可保留仍有价值的内部事实、数学和执行安全工具。
- 代码验证事实并保护不可逆执行；LLM 负责投资判断。
- 没有真实失败证明必要时，不新增 Skill、契约、状态层、评分引擎或框架。
- 本次结构变化发布为插件分发版本 `0.7.0`，不改变投资参数、阈值、公式、投资宇宙或授权语义。

## 持续有效的分发与权威决定

- **Skill 分发版本与政策版本解耦。** `.plugin-version` 服务安装、升级和回滚，不是投资政策版本。
- 默认分支 HEAD 是现行政策的规范来源；运行会话只读取已安装分发的不可变快照，不在运行时 clone、fetch 或自证“最新”。
- 发布 tag 负责把分发版本映射到来源 commit；会话本身不解析 tag。
- `07-Releases/` 已退役，不得恢复第二套发布或政策历史。
- **真实 Agent 行为 Eval 尚未验证**时，静态测试通过不得被描述为 Harness 行为通过。
- 仓库保存规则和公开工具，永不保存个人组合、账户数据、授权或执行回执。

## 新决定模板

```markdown
## YYYY-MM-DD — 决定标题

- 决定：
- 原因：
- 对现行政策或产品边界的影响：
- 规则净增减（宪法复杂度规则要求）：
- 证伪或复核条件：
- 非交易声明：
```
