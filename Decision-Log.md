# Decision Log

只记录会长期改变产品方向、政策权威或运行边界的决定。实现过程、文件迁移、检查器合并、行数统计和已关闭缺陷留在 Git 历史与 PR 中。

## 2026-08-29 — 配置改为四个显式目标权重；SOXX 降为普通持仓；护栏与基准体系精简

- 决定：
  1. **取消 57% 合并袖套**，四个标的各自持有独立目标权重：现金 15%、SPYM 50%、QQQM 30%、SOXX 5%，合计恒为 100%。删除派生符号 `A_basis`、`A_stage`、`U`（阶段储备）。此后任何输出不得再使用「SPYM 与 X 合计多少」这类合并口径。
  2. **新增带宽**：现金 10–20%、SPYM 45–55%、QQQM 25–35%，统一 ±5pp。带宽只用于状态披露与转型完成判定，**不是无交易区**——例行 DCA 与战略基线仍然有正缺口就买、不设阈值。SOXX 不设带宽（对 5% 仓位无意义），达标判据是正缺口为 0。
  3. **现金的三条线拆开**：目标 15%（战略剩余 `S` 的扣除基数）、带宽 10–20%（状态）、硬执行下限 12%（约束交易）。原文把下限与带宽下沿写成同一句，导致调整带宽会连带移动风控线。
  4. **SOXX 降为普通持仓**：目标 5%，缺口由 D / B / 回撤部署与其他标的同等填充。删除执行档序列（3%→4.5%→6%）、回补至目标路径、提高倾斜五步闸门、板块倾斜框架与持仓登记表。
  5. **移除三条穿透护栏**（IT 45/50%、半导体 15%、单一发行人 8/10%）与季度穿透手工核查程序。集中度由所有者在执行层掌握。
  6. **基准体系收敛到 SB-2 一条**（100% SPYM 含分红总收益 + 相同月度入金时点），删除 Policy Benchmark、影子基准 SB-1 及其月度现金利息模型。
  7. 回撤四档阶梯（1.5 / 3 / 4.5 / 6 = 15pp，绝对下限 0%）**不动**。
  8. 同步删除三份 references 中的跨文件重复条款（回撤执行细则、警报指针不变量、月度三张重叠清单、数据契约目录与已退役的每周估值读取流程）。

- 原因：原配置把 SOXX 当作需分阶段审批的板块倾斜治理，为此维护执行档、阶段储备 `U`、回补路径、五步闸门与季度穿透核查五套机械。这些机械服务的前提是「倾斜权重会逐步长大」。新配置把四个目标权重钉死，系统只买正缺口、从不为再平衡卖出，组合的行业与发行人集中度因此是这组固定权重的**直接结果**，在设定权重时即已决定，不会因系统的任何例行动作升高；事前闸门没有可管控的对象。

  **使用 ETF 而非个股消除的是单一公司风险，不构成移除护栏的理由**——ETF 是市值加权且相互重叠，穿透集中度正是由它们产生的。护栏移除的依据只有「权重固定后闸门无对象」这一条。

  Policy Benchmark 的月度现金利息模型依赖逐档利率、免息门槛与 NAV 比例规则，重建成本高、实际使用率低；SB-2 已覆盖「整套系统是否跑赢最简方案」这一核心问题，现金拖累与配置效果都含在该对比中。

- 对现行政策或产品边界的影响：投资宇宙（SPYM / QQQM / SOXX 三标的封闭）不变，回撤部署条款不变，无杠杆红线不变，授权语义不变（候选不等于授权，执行仍需所有者单次授权与 read-back）。改变的是目标权重、带宽、SOXX 的治理类别、护栏的存废与基准口径。

  **行为差异**：权重漂移时旧规则让 SPYM 目标随 SOXX 上涨而缩小（漂移不被稀释）；新规则四个目标固定，漂移标的正缺口为 0、新增资金流向其余缺口，**漂移由稀释逐月修复**。这与「不自动卖出、靠稀释修复」的既定意图一致。

- 规则净增减（宪法复杂度规则要求）：三份 references 1424 → 992 行（净 −432，−30%）：宪法 476 → 275、操作手册 617 → 543、数据契约 331 → 174。可执行镜像 964 → 799 行（净 −165）：`check_policy_consistency.py` 299 → 231、`monthly_execution.py` 665 → 568。新增规则仅带宽 3 条与现金三线拆分 1 条；删除的规则层级包括执行档治理、回补路径、倾斜闸门、板块倾斜框架、持仓登记表、三条护栏、穿透核查程序、Policy Benchmark 与 SB-1，合计净减。

- 证伪或复核条件：
  1. 本次决定所依据的集中度估算（50/30/5 下合并半导体暴露估算约 23.5%、IT 约 44%）**从未经实测穿透核查验证**，只是用宪法旧版记录的两个 2026-07 锚点反推。首次年度审核应实测一次确认。
  2. 若底层基金自身成分漂移（例如 Nasdaq-100 半导体权重上升）使合并暴露显著偏离上述估算，而系统已无机制察觉，应重新审议是否恢复季度穿透核查——**作为披露项，不作交易闸门**。
  3. 若 SOXX 作为普通持仓被例行 DCA 填满后出现未预期的集中度问题，恢复某种形式的加仓闸门并重新记账。
  4. 若带宽被发现事实上当成了无交易区使用（入金因「在带宽内」而滞留现金），说明第 2 项决定的表述不足，需改写。
  5. 本条同时取代 2026-08-04 那条记录中关于「回补至目标」治理路径的部分——该机制已删除；该条记录中「档位已执行判定以成交记录为主信号」的部分**继续有效**。

- 非交易声明：本决定只修改政策文件与可执行镜像，不读取账户、不授权任何订单、不构成买卖建议。实际调仓（若需要）须另行走月度流程或 IC 清单，并由账户所有者在 IBKR 亲手确认。

## 2026-08-04 — 回撤档位判定改以成交记录为主信号；回补至目标免 IC 但每次披露暴露

- 决定：
  1. 档位已执行判定改以**周期内 IBKR 成交记录**为主信号（按金额对应梯度定额、日期在达档之后归属）；现金水位降为辅助校验且不得单独判定；警报指针仍只作 actual pointer 被审计。
  2. 半导体 15% 护栏线只 gate **提高倾斜**；**回补至目标**走例行路径免 IC（不扩大风险预算，上限仍是既有执行档），但每次回补必须报告交易后合并半导体暴露（原「首次」改为「每次」）。
- 原因：
  1. 现金权重分母是实时 NAV，回撤中 NAV 下跌使现金权重机械上升，部署后读数系统性偏高。按本梯度测算，`DD=10%` 执行 T1 后现金约 14.9%（表值 13.5%）、`DD=25%` 打光 T4 后约 13.1%（表值 0%），四档全部偏向「更少档已执行」，会反复重触发浅档，把弹药消耗在浅回撤——正是 1:2:3:4 梯度要防止的失败。成交记录是唯一既独立于 NAV 漂移、又不让被审计对象自证的证据。
  2. Core 自身半导体暴露即约 18%，该线常态触发；而回补按定义无法把 `A_actual` 推过既有执行档，本线要防的集中度爬升已由执行档锁死。原文「任何 SOXX 倾斜的新增」与回补五项约束（只列 IT 50%、发行人 10% 两条冻结线）在同一部分内可作两种读法，压力时刻会出现规则不确定。
- 对现行政策或产品边界的影响：投资参数、阈值、公式、投资宇宙与授权语义零变化。改变的是档位状态如何被重建，以及回补的治理路径由「文本歧义」变为「明确的例行路径 + 每次披露」。
- 规则净增减（宪法复杂度规则要求）：手册状态重建部分净 +1 行（删去现金水位映射表 5 行，新增偏差说明与归属规则）；宪法两处为同行改写，无净增。
- 证伪或复核条件：首次真实回撤触发时，必须在当前私有会话核验成交记录归属是否给出了正确档位；若成交归属无法与档位对应（例如同日既有月度 DCA 又有回撤部署且金额接近），该判定路径回 Research 重审。若回补被发现在执行档不变的前提下仍推高了合并半导体暴露，恢复本线对回补的 IC 要求。
- 非交易声明：本决定只改变状态重建的证据优先级与回补的治理路径表述，不改变任何投资参数、阈值、公式或授权语义，不授权任何订单。

## 2026-08-04 — Investment OS 收缩为一个规则 Skill

- 安装产品只公开 `skills/investment-os/SKILL.md` 一个 Agent 入口。
- 编号 references 保存投资政策；其他 `skills/*/` 目录只可保留仍有价值的内部事实、数学和执行安全工具。
- 代码验证事实并保护不可逆执行；LLM 负责投资判断。
- 没有真实失败证明必要时，不新增 Skill、契约、状态层、评分引擎或框架。
- 本次结构变化发布为插件分发版本 `0.7.0`，不改变投资参数、阈值、公式、投资宇宙或授权语义。

## 持续有效的分发与权威决定

- **Skill 分发版本与政策版本解耦。** `.plugin-version` 服务安装、升级和回滚，不是投资政策版本。
- 默认分支 HEAD 是现行政策的规范来源；运行会话只读取已安装分发的不可变快照，不在运行时 clone、fetch 或自证“最新”。
- 发布 tag 负责把分发版本映射到来源 commit；会话本身不解析 tag。
- `07-Releases/` 已退役，不得恢复第二套发布或政策历史。
- **真实 Agent 行为 Eval 尚未验证**时，静态测试通过不得被描述为 Harness 行为通过。
- 仓库保存规则和公开工具，永不保存个人组合、账户数据、授权或执行回执。

## 新决定模板

```markdown
## YYYY-MM-DD — 决定标题

- 决定：
- 原因：
- 对现行政策或产品边界的影响：
- 规则净增减（宪法复杂度规则要求）：
- 证伪或复核条件：
- 非交易声明：
```
